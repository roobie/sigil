[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "codemark"
version = "0.1.0"
description = "Bookmark code locations with context-aware validation"
requires-python = ">=3.10"

[project.scripts]
codemark = "codemark.cli:main"
